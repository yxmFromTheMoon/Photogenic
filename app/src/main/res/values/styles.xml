<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowBackground">@color/white</item>
    </style>

    <style name="AnimActivityStyle" parent="AppTheme">
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@style/TopInTopOut</item>
    </style>

    <style name="TopInTopOut" parent="AppTheme">
        <item name="android:activityOpenEnterAnimation">@anim/slide_down_from_top</item>
        <item name="android:activityOpenExitAnimation">@anim/anim_no</item>
        <item name="android:activityCloseEnterAnimation">@anim/anim_no</item>
        <item name="android:activityCloseExitAnimation">@anim/slide_up_to_top</item>
    </style>

    <style name="SplashAppTheme" parent="AppTheme">
        <item name="android:windowBackground">@drawable/splash_logo_welcome</item>
        <item name="android:windowFullscreen">true</item>
    </style>

</resources>
